target:
  name: "Default Config"
  type: api
  context:
    purpose: "Testing chatbot"
  settings:
    url: "https://example.com"
    response_selector: "response"
    request_body_template: '{"message": "{{prompt}}"}'
options:
  scanning_agent: "test-scanning-agent-id"
